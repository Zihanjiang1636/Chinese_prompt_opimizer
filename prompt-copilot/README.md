# Prompt Copilot Assets

这个目录存放 Prompt Copilot 的领域资产，方便后续整体摘出：

- `datasets/`：固定中文评测集和真实回归集
- `rubric/`：评分维度与场景权重
- `prompt-templates/`：版本化提示模板
- `reports/`：回归输出
- `notes/`：实验和回归说明

这些文件默认与用户历史记录分离，不和 `data/users/` 下的会话文件混放。
